ВЫБРАТЬ
	НоменклатураСегмента.Сегмент,
	НоменклатураСегмента.Номенклатура
	
ИЗ
	РегистрСведений.НоменклатураСегмента КАК НоменклатураСегмента
ГДЕ
	НоменклатураСегмента.Номенклатура = &Номенклатура
	И НоменклатураСегмента.Сегмент = &Сегмент


000000284 - Справочники.сегментыНоменклатуры.НайтиПоКоду("000000284")


РЛ0000027

000000003


Запрос = Новый Запрос;
Запрос.Текст = "";
Запрос.УстановитьПараметр("", );

РезультатЗапроса = Запрос.Выполнить();
Пусто = РезультатЗапроса.Пустой();

Если Не Пусто Тогда
	Выборка = РезультатЗапроса.Выбрать();
	//Результат = РезультатЗапроса.Выгрузить();
	Пока Выборка.Следующий() Цикл
	КонецЦикла;
	Иначе
КонецЕсли;
